version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npx prisma generate
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*

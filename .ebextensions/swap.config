commands:
  00_setup_swap:
    command: "sudo dd if=/dev/zero of=/swapfile bs=1M count=512 && sudo chmod 600 /swapfile && sudo mkswap /swapfile && sudo swapon /swapfile"
        test: "[ ! -f /swapfile ]"
